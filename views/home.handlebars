<h1>Dixe aqui seu comentario</h1>
<p>A sua opinião é muito importante para nós! </p>
<div id="comentar"<a href="/fomr">Comentar</a></div>

{{#each comments}}
    <h3>{{titulo}}</h1>
    <h4>{{conteudo}}</p>
    <small>{{createdAt}}</small>
    <a href="/delatar/{{id}}"><button>Deletar</button></a>
    <hr></div>

    {{each}}